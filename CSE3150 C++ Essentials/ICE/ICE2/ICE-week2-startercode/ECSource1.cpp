// A simple source file. What is wrong with it?

// there isn't any return value, main is a function that return an int, so it should return an int
#include <iostream>
#include "ECHeader1.h"

int main()
{
  // do stuff
  cout << "foo: " <<  foo(2) << endl;
}
